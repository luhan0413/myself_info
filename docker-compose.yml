services:
  portfolio:
    build: .
    container_name: portfolio-website
    ports:
      - "413:80"  # 將本地的 0413 端口映射到容器的 80 端口
    volumes:
      - .:/usr/share/nginx/html:ro  # 掛載本地目錄到容器（唯讀模式）
    restart: unless-stopped
